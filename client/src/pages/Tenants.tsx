import React from "react";

const Tenants: React.FC = () => {
  return (
    <div>
      <h1 className="text-xl font-bold mb-2">Tenants</h1>
      <p>Manage your tenants here (CRUD operations + assignments).</p>
    </div>
  );
};

export default Tenants;
